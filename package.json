{
  "name": "app",
  "private": true,
  "scripts": {
    "start": "app start",
    "build": "app build",
    "clean": "app clean",
    "lint": "eslint --max-warnings 0 . && prettier --check .",
    "lint:fix": "eslint --max-warnings 0 --fix . && prettier --write ."
  },
  "dependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@buncho/builder": "^0.9.8",
    "@buncho/eslint-config": "^0.6.0",
    "@buncho/tsconfig": "^0.2.0",
    "@prettier/plugin-xml": "^0.12.0",
    "eslint": "^7.15.0",
    "prettier": "^2.2.1",
    "typescript": "^4.1.2",
    "typescript-plugin-css-modules": "^3.0.1"
  }
}
